#pragma once

#include <QtWidgets/QMainWindow>

class EmbedCalendar : public QMainWindow
{
    Q_OBJECT

public:
    EmbedCalendar(QWidget *parent = nullptr);
    ~EmbedCalendar();

private:
};
